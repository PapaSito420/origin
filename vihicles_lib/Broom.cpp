#include "Broom.h" 

double calculating (double distance) {
	double distanceReduction  = (distance / 1000.0)*0.01;
	double reducedDistance = distance * (1 - distanceReduction); 
	
	return reducedDistance = actualDistance/ speed; 
}